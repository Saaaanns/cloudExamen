name: visualize
on:
  push:
    branches:
    - main
jobs:
  visualize-job:
    runs-on: ubuntu-latest
    steps:
      - name: setup node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - name: repo visualizer
        uses: actions/visualize@v3
